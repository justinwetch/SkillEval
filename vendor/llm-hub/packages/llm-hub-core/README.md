# @llm-hub/core

`@llm-hub/core` provides the reusable multi-provider connection layer for future AI apps.

It includes:

- provider definitions and registry wiring
- auth method handlers
- schema-driven setup UI metadata
- credential storage abstractions
- seed model catalog
- public service methods for connect, disconnect, test, and runtime model access
